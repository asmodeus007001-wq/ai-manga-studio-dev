# AI Manga Studio 개발 요청서 (v2 — 사양 최적화판)

> 이 문서는 원본 요청서를 기반으로, 사용자 PC 사양(RX 6600 8GB)과 "여러 패널로 구성된 만화를 완성한다"는 목적에 맞춰 **모델 선정·Backend·캐릭터 일관성 전략**을 구체화한 버전입니다. 구조와 원칙은 원본을 그대로 유지하고, 실행 가능성이 낮거나 만화 제작 목적과 어긋나는 부분만 조정했습니다.

## 0-A. 실행 전제 조건 (반드시 먼저 확인)

이 요청서는 Manus가 **사용자의 실제 PC에 직접 접근**해서 ComfyUI를 실행하고 벤치마크를 수행한다는 전제로 작성되었습니다. 일반 클라우드 세션의 Manus는 로컬 GPU에 접근할 수 없으므로, 작업을 시작하기 전에:

1. **Manus Desktop(My Computer) 연결 상태**에서 작업을 진행할 것
2. 만약 클라우드 세션에서만 작업 가능하다면, "실제 RX 6600 실측"이 요구되는 항목(#4, #40, #41)은 수행 불가능함을 먼저 사용자에게 알리고 대안(코드/설정만 준비, 실측은 사용자가 직접 수행)을 제안할 것

## 0-B. 작업 전달 방식 — 한 번에 전체를 주지 않는다

이 문서 전체(46개 이상 섹션)를 한 번에 지시하지 않는다. 아래 순서로 **단계별 세션을 나누어** 전달한다. 각 단계 완료 확인 후 다음 단계 문서를 전달한다.

| 단계 | 내용 | 관련 섹션 |
|---|---|---|
| 1 | 환경 조사, 기존 ComfyUI 백업, AMD Backend 확정 | #1, #0-C |
| 2 | 모델 실측 테스트, 체크포인트 확정 | #4 |
| 3 | 기본 Text-to-Image + VRAM 저부하 Workflow | #3, #10 |
| 4 | Reference/ControlNet/IPAdapter 기반 캐릭터 일관성 | #18, #26 |
| 5 | Inpainting/자동 검수 | #22~#25 |
| 6 | 자연어 Scene Parser | #12~#14 |
| 7 | Multi-character | #17 |
| 8 | Manga Editor (페이지/말풍선/대사/SFX/Export) | #32~#35 |

---

## 0-C. AMD Backend 우선순위

RX 6600(아키텍처 gfx1032)은 ZLUDA 공식 지원 목록에 포함되어 있고, 같은 세대 카드(RX 6700/6800) 실측 비교에서 DirectML 대비 **VRAM 점유가 뚜렷이 적고 속도도 빠른** 것으로 보고되어 있다. 따라서 우선순위를 다음과 같이 정한다.

1. **ZLUDA (1순위)** — `patientx/ComfyUI-Zluda` (Windows 전용, RX 6600 계열 지원 확인됨) 를 우선 설치·테스트한다.
2. **DirectML (폴백)** — ZLUDA에서 크래시, 미지원 노드, 드라이버 충돌 등이 발생할 경우에만 전환한다. 모든 AMD GPU에서 동작하지만 느리고 VRAM 소모가 크다.
3. **ROCm / ZLUDA-Linux 계열은 제외** — 사용자 환경은 Windows 11이므로 공식 ROCm Windows 지원이 없는 이상 고려하지 않는다.

실측 시 두 Backend 모두에서 동일한 SDXL 체크포인트로 1024x1024 생성 시간과 VRAM 점유를 기록하고 비교 후 확정한다.

---

## 4′. 모델 선정 (원본 #4 대체)

특정 모델을 처음부터 확정하지 않는다는 원칙은 유지하되, 아래 우선순위로 테스트 순서를 정한다.

### 주력 후보 (1순위 — 만화/애니메 표현 + 태그 정확도)

| 모델 | 계열 | 선정 이유 | 주의사항 |
|---|---|---|---|
| **NoobAI-XL** | Illustrious 계열 | 손·눈·입 아티팩트가 적고 danbooru 태그를 가장 정확히 따름 → Scene Parser가 만든 태그를 그대로 신뢰할 수 있음 | **v-prediction 계열**이므로 ComfyUI에서 `ModelSamplingDiscrete` 노드에 `v_prediction` 설정이 빠지면 결과가 깨짐. Workflow 템플릿에 고정으로 박아둘 것 |
| **WAI-Illustrious-SDXL** | Illustrious 계열 | 설정이 단순하고(Euler a, CFG 5~6) 안정적, v-pred 이슈 없음 | 라이선스/재배포 조건 확인 후 채택 |

### 보조 후보 (2순위 — 빠른 Draft/Preview)

- 위 두 체크포인트 기반 **8~12 step 저CFG Fast Draft Workflow** (같은 화풍 유지한 채로 속도만 줄임 — 초안과 최종 화풍이 달라지는 문제를 피함)
- SDXL Turbo/LCM 계열은 화풍이 애니메 표현에 약하므로, 1순위 모델의 저스텝 버전으로 먼저 시도하고 그래도 느리면 후순위로 테스트

### 실험적 후보 (후순위 — 조건부 채택)

- **Z-Image / Z-Image-Turbo**: 공식 권장 VRAM이 16GB로, 8GB 환경에서는 fp8 양자화 + CPU 오프로드 없이는 사실상 사용 불가. 양자화 버전으로 실측했을 때 속도·품질이 Illustrious 계열보다 명백히 우수할 경우에만 보조 옵션으로 채택. 기본 채택 모델로는 사용하지 않는다.
- Pony Diffusion XL: 결과 편차가 크고 `score_9` 등 score-tag 프리픽스가 강제되어 자동화 파이프라인과 궁합이 나쁨 — 배제

### 공통 실측 항목 (원본 유지)

VRAM 사용량 / 생성 시간 / 이미지 품질 / 캐릭터 표현 / 포즈 제어 / Reference 대응 / Inpainting / AMD 호환성 / 안정성

---

## 26′. Character Consistency (원본 #26 대체)

본인 그림체 LoRA 학습은 사양상 보류하기로 했으므로, **LoRA 없이도 얼굴·신체 일관성을 확보하는 조합**을 기본 전략으로 삼는다.

### 기본 전략 (LoRA 없이)

```text
IP-Adapter FaceID Plus v2 (SDXL)  → 얼굴 고정 (weight 0.70~0.85)
        +
ControlNet (OpenPose)             → 포즈 변경 시 신체 붕괴 방지
        +
고정 Seed + Generation Metadata   → 재현성 확보
```

캐릭터 등록 시 얼굴이 잘 보이는 참조 이미지 1장을 저장해두면, 이후 모든 패널에서 의상/포즈/배경이 바뀌어도 얼굴은 고정된다. 매번 같은 Prompt를 반복하지 않아도 되는 핵심 장치다.

### 확장 전략 (선택 — 특정 주인공만)

- 등장 빈도가 매우 높은 주인공 1~2명에 한해, IPAdapter+ControlNet 결과물 중 잘 나온 이미지 15~50장을 모아 **캐릭터 전용 LoRA**를 학습(로컬 1~4시간 소요, VRAM 여유 있을 때 야간 실행 권장)하면 일관성을 100%에 가깝게 끌어올릴 수 있다.
- 이 확장 전략은 필수가 아니라 옵션이며, 기본 전략만으로 충분한지 먼저 평가한 뒤 결정한다.

### VRAM 동시 로딩 최소화

체크포인트 + IPAdapter + ControlNet + LoRA를 동시에 GPU에 올리면 8GB를 넘기기 쉽다.

- 체크포인트는 **fp8 또는 GGUF 양자화 버전**을 기본으로 사용 (fp16 대비 VRAM 절반 이하)
- CLIP/Text Encoder는 CPU 오프로드
- ControlNet/IPAdapter는 필요한 패널에서만 순차 로딩 후 언로드

---

## 신규. 패널 생성 방식 — 개별 생성 vs 배치 생성

"낱개로 생성하든 여러 장을 한 번에 생성하든 방법은 상관없다"는 요구사항에 대해, 실측 근거를 바탕으로 다음 전략을 기본값으로 채택한다.

- **하나의 캔버스에 여러 컷을 한 번에 그리게 하는 방식(멀티패널 동시 생성)은 채택하지 않는다.** 캐릭터 속성이 컷 사이에서 섞이는(Character Attribute Swap 등, 원본 #17에서 이미 지적한 문제) 사고가 훨씬 잦다.
- **패널은 1장씩 순차 생성**하되, 직전 패널의 캐릭터 참조/Seed/Metadata(#28)를 그대로 이어받아 사용한다.
- "배치 생성"은 **같은 패널의 후보 여러 장을 동시에 뽑아 그중 하나를 고르는 용도**로만 사용한다 (예: 같은 장면을 4장 동시 생성 후 검수/선택).

---

## 신규. 출력 형식 설정 (프로젝트 생성 시 결정)

원본 문서에는 최종 결과물이 세로 스크롤 웹툰인지 페이지 넘김식 만화책인지, 컬러인지 흑백 선화+톤인지가 명시되어 있지 않다. 이는 Page/Panel 구조(#33)와 Export 방식에 직접 영향을 주므로, **프로젝트 생성 시 아래 항목을 사용자가 선택**하도록 UI에 추가한다. (지금 문서에서 하나로 못박기보다, 프로젝트별로 유연하게 선택 가능하도록 설계)

```text
Project Settings
├── Layout Type: [세로 스크롤 웹툰] / [페이지 넘김 만화책]
├── Color Mode: [컬러] / [흑백 선화 + 톤]
└── Export Resolution / Page Size
```

---

## 그 외 원본 섹션은 그대로 유지

VRAM 8GB 제약(#3), 기존 ComfyUI 환경 보호(#1), Semantic Scene Parser(#13~14), 캐릭터 속성 분리(#16), Reference 시스템(#18), Targeted Inpainting(#22~23), Automatic Visual Verification(#24~25), Manga Editor(#32~35), 완료 기준(#44, 단 MVP와 확장 기능으로 분리 권장) 등 원본의 나머지 섹션은 수정 없이 그대로 적용한다.

**완료 기준 관련 권장사항**: #44의 80개 이상 체크리스트를 한 번에 요구하지 말고, 아래처럼 MVP와 확장으로 나눈다.

- **MVP**: Basic Text-to-Image + Character Reference(IPAdapter+ControlNet) + 기본 Inpainting(손/얼굴) + 1페이지 Export
- **확장**: Multi-character, 자동 검수 루프, 캐릭터 전용 LoRA, Story State, Generation History 전체
