# AI Manga Studio 요청서 v6 → v7 수정 제안

> 이 문서는 v6 원본을 대체하지 않습니다. 아래 섹션들을 v6의 해당 위치에 병합하거나 추가하는 방식으로 사용하세요.

---

## 0. 왜 이 수정이 필요한가 (배경 — Manus에게 전달할 필요는 없음)

1단계 실행 결과, 다음 네 가지 구조적 전제가 드러났습니다.

- Manus 채팅 인터페이스는 **1일 최대 4개 파일**까지만 전송 가능하다(그 이상은 다음 날까지 대기).
- **실측(GPU 생성, Windows 설치, SmartScreen 등)은 전부 Owl님이 직접 로컬 PC에서 수행**해야 한다. Manus는 Owl님의 PC에 원격으로 접근할 수 없다.
- 모든 단계마다 Owl님이 직접 테스트하는 것은 비효율적이며, **필수적인 지점에서만** 테스트가 이루어져야 한다.
- 최종 산출물은 **게임처럼 설치가 쉽고, Windows "설정 → 앱"에서 한 번에 완전히 지울 수 있는 설치 프로그램**이어야 한다 — 특히 이 프로젝트는 여러 차례 수정하면서 재설치/삭제를 반복하게 되므로, 이 부분이 편해야 개발 과정 자체가 수월해진다.

이를 반영해 v6의 §1(작업 방식 원칙), §6(Model Manager), §12(단계 순서), §13(DoD)에 아래 내용을 추가/수정합니다.

---

## §1-보강. 실행 환경 및 협업 방식 원칙 (v6 §1에 추가)

5. **Manus 실행 환경 선언**: Manus는 GPU가 없는 Linux 클라우드 샌드박스에서 동작하며, Owl님의 실제 Windows 11 + AMD RX 6600 PC에 원격으로 접근할 수 없다. 따라서 이 문서의 모든 실측·검증 요구사항은 다음 두 그룹으로 나뉜다.
   - **[Manus 자체]**: Manus 샌드박스 안에서 코드 실행·단위 테스트·정적 검증으로 완결 가능한 항목. Manus가 직접 실행하고 로그를 제출한다.
   - **[Owl PC 필수]**: 실제 GPU 생성, Windows 설치/제거, SmartScreen 등 Owl님의 로컬 PC에서만 확인 가능한 항목. Manus는 이 항목에 대해 코드·스크립트·설치파일까지만 만들고, Owl님이 실행한 결과(스크린샷/로그)를 별도로 전달받아 최종 판정한다.
   - 이 문서의 §13 DoD 각 항목에는 이 태그가 표시되어 있다(§13-보강 참고).

6. **단일 소스 저장소 원칙**: 이 프로젝트의 모든 코드, 요청서, 진행 상태는 Git 저장소 `https://github.com/<Owl님-계정>/ai-manga-studio-dev` (아래 §부록 절차로 생성) 하나를 단일 소스로 사용한다. **git push/pull은 채팅 파일 전송과 별개의 통로이므로, 1일 4개 파일 한도에 전혀 영향을 받지 않는다** — 이게 이 원칙을 쓰는 핵심 이유다.
   - Manus는 **매 세션 시작 시 가장 먼저** 이 저장소를 clone(최초) 또는 pull(이후)하고, 성공/실패 여부를 세션의 첫 응답에 보고한다. 실패 시 즉시 예외 보고하고 지시를 기다린다.
   - 저장소 안의 `PROGRESS.md`를 먼저 읽고 이전 단계까지의 상태를 파악한 뒤, 이번에 지시받은 단계 섹션만 실행한다.
   - **대용량 바이너리(모델 체크포인트, LoRA, .safetensors, .bin, .onnx 등 수십 MB 이상 파일)는 이 저장소에 절대 커밋하지 않는다.** 모델은 §6(Model Manager)의 다운로드 링크 정보로만 다루고, 실물 파일은 Owl님 PC가 직접 받는다.
   - 저장소에 없는 기존 산출물이 필요한 단계인데 저장소에도 없다면, 진행하지 말고 예외 보고 후 Owl님에게 입력을 요청한다.
   - **저장소를 만들고 싶지 않다면**: §부록-B "마스터 패키지 zip" 방식으로 대체한다. 이 경우 매 게이트마다 Owl님이 그 시점까지의 저장소 내용을 압축파일 1개로 새로 묶어 전달한다(1일 4개 한도 안에서 충분히 처리 가능).

7. **단계 종료 시 `PROGRESS.md` 갱신 의무화**: 완료 항목, 남은 작업, 이번 단계에서 발견된 이슈, 다음 세션이 알아야 할 컨텍스트를 반드시 갱신하고 커밋한다.

8. **설치 프로그램 요구사항 강화(§10 보강)**: 최종 설치 파일은 "게임처럼" 설치·삭제가 쉬워야 한다. 이는 최종 사용자 경험을 위한 것이기도 하지만, **개발 중 Owl님이 반복적으로 재설치/삭제하며 테스트해야 하는 것 자체를 위한 요구사항**이기도 하다. Inno Setup(§2-10 확정값)은 Windows "설정 → 앱" 표준 제거를 기본 제공하므로 이 목적에 정확히 부합한다 — 이 결정은 그대로 유지하되, 이유를 이렇게 명시적으로 못박는다.

---

## §12-보강. 필수 검증 게이트 (v6 §12 단계표 대체 또는 병기)

모든 단계에서 Owl님이 테스트하지 않는다. 아래 5개 게이트에서만 실제 PC 테스트를 수행한다. 게이트가 아닌 단계는 Manus가 자체 검증까지 마치고 넘어간다.

| 게이트 | 커버 단계 | Manus가 자체 완결 | Owl님이 반드시 확인 |
|---|---|---|---|
| (자체 완결, 게이트 아님) | 2단계 Qwen3 NLU | CPU 추론이라 샌드박스 자체 테스트·모델 비교까지 가능 | — |
| (자체 완결, 게이트 아님) | 4단계 백엔드 코드, 6·7단계 UI 구조 | 로직/API·죽은 버튼·레이아웃 정합성 | — |
| **게이트 1** | 3단계 말미 | 그래프 검증 스크립트 작성·자체 테스트 | 4개 워크플로우 `/prompt` 실제 큐 실행 결과 확인 |
| **게이트 2** | 4~5단계 말미 | — | 캐릭터 일관성(여러 패널) + Inpainting 결과를 한 번에 같이 확인 |
| **게이트 3** | 6~7단계 말미 | — | 전체 UI + Manga Editor 실사용 시나리오 1회 통합 테스트 |
| **게이트 4** | 8~9단계 | — | 설치/제거 + SmartScreen을 한 번의 설치 사이클로 같이 확인 |
| **게이트 5** | 10~11단계 | — | 확장 기능 + 최종 통합 테스트 |

게이트에서 Owl님이 제출할 때는 다음 3종 세트로 고정한다: **(1) 스크린샷, (2) 콘솔/로그 파일, (3) 실행한 정확한 명령어 또는 조작 순서.**

---

## §6-보강. 모델 자동 설치 실패 시 수동 삽입 UX (v6 §6-2, §6-3 구체화)

Model Manager 화면의 각 "Missing" 모델 카드에는 다음 4개 버튼을 반드시 포함한다.

1. **[파일명 복사]** — 정확한 파일명을 클립보드로 복사
2. **[다운로드 페이지 열기]** — 공식 다운로드 URL을 새 브라우저 탭에서 열기
3. **[저장 폴더 열기]** — 해당 모델의 정확한 목표 경로(`ComfyUI/models/ipadapter/` 등)를 Windows 탐색기로 즉시 열기(`explorer.exe <path>`) — 사용자가 다운받은 파일을 끌어다 놓기만 하면 되게 함
4. **[재스캔]** — 폴더 내용을 다시 검사해 Installed/Missing 상태 갱신

Civitai(NoobAI-XL 등) 처럼 **로그인/연령 확인/라이선스 동의가 필요한 모델은 자동 다운로드를 아예 시도하지 않고**, 처음부터 위 수동 절차로 안내한다(시도 후 실패 메시지가 뜨는 방식보다 우선).

이 기능은 GPU가 필요 없는 UI/파일시스템 로직이므로 Manus가 대부분 자체 검증 가능하며, 실제 최종 확인은 게이트 4~5에서 함께 처리한다.

---

## §13-보강. DoD 항목별 검증 주체 태그 (v6 §13에 태그 추가 예시)

기존 MVP DoD 목록의 각 항목 앞에 아래처럼 태그를 붙인다 (전체 재작성이 아니라 태그만 추가).

- `[Manus 자체]` Qwen3-8B GGUF Q4_K_M 로컬 파싱 파이프라인 동작, EXAONE/Bllossom 비교 근거
- `[Manus 자체]` 생성 전 프롬프트 미리보기/수정 UI 동작
- `[게이트 1]` `통합_자연어_IPAdapter_ControlNet` 재구성 + 그래프 검증 스크립트 통과 + `/prompt` 실제 큐 실행 성공 (4개 전부)
- `[Manus 자체]` `patch_*.py` 정리 후 코드 구조 설명
- `[게이트 2]` §2-4 파일 세트로 Character Consistency 여러 패널 연속 생성 확인
- `[게이트 2]` Generation Metadata/History/Undo 동작 확인
- `[게이트 2]` Targeted Inpainting이 지정 영역 밖을 보존하는지 확인
- `[Manus 자체]` UI 전 항목 ↔ 실제 기능 매핑 체크리스트
- `[게이트 3]` Manga Editor와 Export(롱스트립/PDF) 동작 확인
- `[Manus 자체]` 프로젝트 Export/Import 동작 확인
- `[게이트 1]` 기존 ComfyUI 환경 손상 없이 보존됨 확인
- `[Manus 자체]` `validate_stage2.py` 기준 커스텀노드/모델 설치 확인 결과 (경로·존재 여부까지는 자체 확인, 실제 로딩은 게이트 1에서)
- `[게이트 1]` NoobAI-XL + `sdxl-vae-fp16-fix` 적용 확인(검은 이미지/NaN 없음)
- `[게이트 1]` `patientx-cfz/comfyui-rocm` 설치 및 실제 안정성 확인

확장 DoD도 동일하게: LoRA 학습 파이프라인 `[게이트 5]`, Inno Setup 설치 `[게이트 4]`, 앱 제거 `[게이트 4]`, SmartScreen 캡처 `[게이트 4]`, Multi-character 자동검사·VLM 검수 `[게이트 5]`.

---

## 부록-A. Git을 명령어 없이 쓰는 방법 (권장) — GitHub Desktop

터미널 명령어 없이, 마우스 클릭과 드래그앤드롭만으로 끝납니다.

1. [github.com](https://github.com) 계정 생성(무료) → 로그인
2. github.com에서 "New repository" 클릭 → 이름 `ai-manga-studio-dev` → Private 선택 → Create
3. [desktop.github.com](https://desktop.github.com)에서 **GitHub Desktop** 설치(Windows용) → 로그인
4. GitHub Desktop에서 "Clone a repository" → 방금 만든 저장소 선택 → PC의 폴더 하나로 내려받아짐
5. 그 폴더 안에 이번 대화에서 받은 v7 요청서, 기존 워크플로우 JSON 4개(수정된 통합 워크플로우 포함), patch 파일들, DxDiag.txt 등을 **탐색기에서 그냥 끌어다 놓기**
6. GitHub Desktop 화면 왼쪽에 변경된 파일 목록이 자동으로 뜸 → 하단에 커밋 메시지("초기 자료 업로드") 입력 → **Commit to main** 클릭 → **Push origin** 클릭
7. 완료. 이제 Manus에게는 채팅으로 파일을 보낼 필요 없이 **저장소 URL만 알려주면** 됩니다.
8. 이후 파일을 추가하고 싶을 때도 3~4번 반복(폴더에 끌어다 놓기 → Commit → Push)만 하면 되고, 이건 **하루 4개 파일 제한과 무관**합니다.

## 부록-B. Git 없이 진행하는 방법 (대안)

계정을 만들기 싫으시면, 저 대신 매 게이트 직전에 **그 시점까지 필요한 파일을 전부 압축한 zip 1개**를 만들어드리겠습니다. Owl님은 그 zip 1개만 Manus에게 전달하면 되므로(1일 4개 중 1개만 사용), 한도 문제는 발생하지 않습니다. 다만 이 방식은 Manus가 이전 세션에서 만든 코드를 Owl님이 매번 받아서 저에게 다시 넘겨줘야 이어붙일 수 있다는 번거로움이 있어, 결과적으로 부록-A보다 손이 더 많이 갑니다.

## 최초 1회 필요한 작업 (Owl님)

1. 부록-A(권장) 또는 부록-B 중 방식 선택
2. (부록-A 선택 시) 위 1~7단계 진행 후 저장소 URL을 알려주시면, 이 v7 문서의 저장소 URL 자리를 채워드리겠습니다
3. 이 문서(v7 수정 제안)를 v6에 병합한 최종본을 저장소 루트(또는 zip)에 포함
4. Manus에게 "저장소 URL(또는 첨부 zip) + 1단계부터 다시 시작" 지시
