# AI Manga Studio — 최종 통합 개발 요청서 (v3, UI/패키징/배포 보완판)

> 이 문서는 기존에 전달했던 두 문서
> ①「AI Manga Studio 개발 요청서 v2」(모델·백엔드·캐릭터 일관성 사양)
> ②「Manus 최종 보완 요청서」(한국어 NLU, 워크플로우 링크 오류, 검증 절차)
> 의 내용을 **그대로 승계**하면서, 실사용 중 새로 확인된 3가지 문제
> (UI 부재 / 배포 방식 부적합 / 보안 경고 미대응)를 추가로 명시한 **최종본**입니다.
> 이 문서 하나만으로 처음부터 끝까지 작업이 가능하도록 작성했으니, 이번 세션에서는 이 문서를 기준으로 진행해 주세요.

---

## 0. 작업 원칙 (반드시 지킬 것)

1. **이 문서를 한 번에 다 던지지 말고**, 아래 순서대로 단계별 세션으로 나눠서 진행한다. 각 단계가 끝나면 결과물(코드/스크린샷/검증 로그)을 먼저 보여주고, 확인 후 다음 단계로 넘어간다.
2. 애매하게 해석될 수 있는 부분이 있으면 **임의로 추측해서 만들지 말고**, 먼저 질문한다. (이게 지금까지 계속 재작업이 발생한 원인이었음)
3. 아래 "완료 기준(DoD)"에 명시된 항목을 스스로 체크리스트로 만들어서, 각 항목이 됐는지/안 됐는지 표시한 뒤 결과와 함께 제출한다.

| 단계 | 내용 |
|---|---|
| 1 | 기존 산출물(코드/워크플로우 JSON) 인벤토리 정리 + `patch_*.py` 통폐합 구조 설계 |
| 2 | 한국어 NLU(로컬 LLM) 파이프라인 구현 + 프롬프트 미리보기 UI |
| 3 | 워크플로우 그래프 검증 스크립트 + `/prompt` 실제 큐 테스트 |
| 4 | 전체 UI(아래 3장) 구현 — 기능별로 쪼개서 진행 |
| 5 | 패키징(설치 프로그램) 구현 + 설치/삭제 테스트 |
| 6 | 통합 테스트 + 최종 보고 |

---

## 1. 지금까지 확정된 사양 — 그대로 유지 (재검토 불필요)

이 항목들은 이미 검증이 끝났으므로 **다시 바꾸거나 재조사하지 말 것**.

- **백엔드**: ROCm 네이티브로 이미 정상 동작 확인됨 (`PyTorch 2.12.0+rocm7.14.0`, `AMD Radeon RX 6600 : native`, VRAM 7.84GB free). ZLUDA/DirectML 추가 설치 불필요.
- **체크포인트**: Animagine XL 4.0 계속 사용. 시간 되면 NoobAI-XL / WAI-Illustrious-SDXL과 품질 비교만 추가로 진행 (교체 필수 아님). NoobAI-XL 계열 사용 시 `ModelSamplingDiscrete`에 `v_prediction` 설정 누락되지 않도록 워크플로우 템플릿에 고정.
- **캐릭터 일관성**: IP-Adapter FaceID Plus v2(weight 0.70~0.85) + ControlNet(OpenPose) + 고정 Seed/메타데이터. LoRA 학습은 선택 옵션으로만 남겨둔다.
- **패널 생성 방식**: 멀티패널 동시 생성 금지, 1장씩 순차 생성(직전 패널의 참조/Seed 이어받기). "배치 생성"은 같은 컷의 후보 여러 장을 뽑아 고르는 용도로만 사용.
- **필요 커스텀 노드/모델**: `ComfyUI_IPAdapter_plus`, `comfyui_controlnet_aux`, `ComfyUI-Impact-Pack`, `ComfyUI-Impact-Subpack`, `OpenPoseXL2.safetensors`, `bbox/face_yolov8m.pt`, `bbox/hand_yolov8s.pt` — 설치 여부를 `validate_stage2.py`로 확인 후 결과 보고.

---

## 2. 아직 해결 안 된 핵심 결함 3가지 — 반드시 이번에 해결

### 2-1. 한국어 자연어 이해 방식 교체 (최우선)

- 지금처럼 고정된 80개 구문 사전(`translate_helper.py`) 방식을 **기본값에서 제거**한다.
- **기본(항상 동작)**: 로컬 한국어 지원 소형 LLM(Qwen2.5 계열 / EXAONE / Bllossom 등 GGUF 양자화)을 llama.cpp로 **CPU에서** 구동, 한국어 문장 → 장면 구조(인물/포즈/의상/시선/배경/카메라) 분석 → 영어 프롬프트 변환. (RAM 32GB, 이미지 생성은 GPU라 리소스 충돌 없음)
- **선택**: `OPENAI_API_KEY`가 있으면 우선 사용 가능하지만, **없을 때도 로컬 LLM 경로로 항상 정상 동작해야 함**. "키 없으면 사전 매칭으로 조용히 다운그레이드"는 금지.
- 사전에 없는 표현을 그냥 버리지 말고 "이 부분은 이렇게 해석했다"를 반드시 표시.
- 처리 경로(로컬 LLM/OpenAI)를 화면에 항상 표시하고, **생성 버튼을 누르기 전에** 최종 영어 프롬프트를 미리 보여준 뒤 사용자가 확인/수정할 수 있게 한다. (구체적 UI 위치는 3장 참고)

### 2-2. 워크플로우 그래프 링크 오류 재발 방지

- 이전에 확인된 `통합_자연어_IPAdapter_ControlNet.json`의 오류(`KSampler.model`이 LoadImage의 IMAGE 출력에 잘못 연결, `IPAdapterAdvanced.image`가 KSampler의 LATENT 출력에 잘못 연결)는 아래처럼 고정한다.
  ```
  KSampler.model         ← IPAdapterAdvanced(#6)의 MODEL 출력
  IPAdapterAdvanced.image ← LoadImage(#5, 캐릭터 참조 이미지)의 IMAGE 출력
  ```
- 이후 워크플로우 JSON을 만들거나 수정할 때마다, **출고 전에 반드시** 아래 두 가지를 통과시키고 결과를 같이 제출한다.
  1. 각 노드 입력의 `link` id가 가리키는 링크의 target이 실제로 그 노드가 맞는지, 타입이 일치하는지 검사하는 스크립트(`fix_workflow_links.py`, `validate_stage2.py` 재사용/확장)
  2. 실제로 ComfyUI API `/prompt`에 큐를 넣어 에러 없이 끝까지 실행되는지 확인 (JSON 문법이 맞다고 그래프가 맞는 게 아니라는 점이 이미 확인됨)

### 2-3. 코드 구조 정리 (patch_*.py 근본 해결)

- `patch_*.py`, `fix_*.py` 류가 50개 가까이 누적된 상태를 이번 최종본에서 **한 번에 정리**한다. 특히 UI 관련(`patch_white_ui.py`, `patch_ui_final_polish.py`, `patch_cache_blank_screen.py`, `fix_removed_scene_button.py` 등)은 같은 문제가 반복 발생했다는 뜻이므로, 패치를 계속 쌓지 말고 **원인을 근본적으로 고친 단일 코드베이스**로 재작성한다.
- 4장의 UI를 새로 만드는 이번 작업이 이 리팩터링을 자연스럽게 병행할 좋은 시점이다.

---

## 3. UI/UX 사양 (신규 — 이번 요청의 핵심)

### 3-0. 왜 필요한가

지금까지는 UI 요구사항이 없어서 기능이 만들어져도 어디 있는지 못 찾거나, 버튼이 죽어있거나(눌러도 반응 없음), 화면 구성이 중구난방이었다. 이번에는 **레이아웃과 배치까지 명시**하니 이 구조를 그대로 따라서 만든다.

### 3-1. 참고 레퍼런스

레이아웃 참고: `https://betterwaifu.com/ko/create` (AI 이미지 생성 웹 UI의 화면 구성·정렬 방식만 참고할 것 — 해당 사이트의 콘텐츠·문구·브랜딩과는 무관하며, 우리 프로젝트는 **만화/웹툰 제작 툴**이라는 목적에 맞게 문구와 기능을 새로 구성한다.)

참고할 구조적 요소:
- 화면을 **좌측 컨트롤 패널 / 중앙 캔버스(미리보기) / 우측 기록·갤러리 패널**의 3분할로 구성
- 좌측 패널은 위에서 아래로 "무엇을 그릴지 → 어떻게 그릴지 → 참조 이미지 → 출력 형식 → 고급 설정 → 생성 버튼" 순서로 논리적 흐름을 따라 배치
- 각 옵션 카테고리(모델/확장기능/캐릭터/스타일 등)는 접었다 펼 수 있는 섹션(collapsible)으로 구분해서, 화면이 복잡해 보이지 않으면서도 다 찾을 수 있게 함
- 상단에 페이지 전환 탭(만들기 / 내 작업 기록 / 프로젝트) 고정

### 3-2. 화면 구성 상세

**A. 상단 바 (고정)**
- 좌: 로고/프로젝트명, 현재 프로젝트 선택 드롭다운
- 우: [만들기] [패널 편집기] [내 작업 기록] 탭 전환, 다크모드 토글

**B. 좌측 컨트롤 패널 (스크롤 가능, 섹션별 접기/펼치기)**

| 섹션 | 포함 기능 | 비고 |
|---|---|---|
| 1) 한국어 프롬프트 입력 | 자연어 텍스트박스 + "해석 보기" 버튼 | 눌렀을 때만 하단에 해석 결과(장면 구조 태그 + 처리 경로: 로컬LLM/OpenAI 표시)를 펼쳐서 보여줌. 여기서 직접 태그 수정 가능 |
| 2) 캐릭터 참조 | 등록된 캐릭터 목록(썸네일+이름), 새 캐릭터 등록(참조 이미지 업로드), 이번 패널에 적용할 캐릭터 선택(다중 가능) | IP-Adapter 참조 이미지와 연결 |
| 3) 워크플로우/모델 | 워크플로우 자동 선택 표시(기본생성 / img2img / IPAdapter+ControlNet / Detailer 중 현재 입력에 맞게 자동 판단된 것을 보여주고, 수동 오버라이드 가능), 체크포인트 선택 | 사용자가 워크플로우 종류를 몰라도 되게, 기본은 "자동"으로 두고 필요하면 수동 전환 |
| 4) 포즈/구도 | ControlNet(OpenPose) 참조 이미지 업로드 또는 프리셋 포즈 선택 | |
| 5) 출력 형식 | 레이아웃(세로스크롤 웹툰 / 페이지 만화책), 컬러모드(컬러 / 흑백+톤), 컷 비율 프리셋 버튼(4:5, 2:3, 9:16, 1:1 등) + 커스텀 | 프로젝트 생성 시 1회 설정, 패널마다 필요하면 변경 가능 |
| 6) 생성 매수 | "1장" / "후보 4장 중 선택" 토글 | 배치 생성은 후보 선택 용도로만 |
| 7) 고급 설정 (기본 접힘) | Steps, CFG, Sampler, Seed(고정/랜덤), v-prediction 토글 | 초보자는 안 열어봐도 되게 기본값으로 정상 동작 |

- **생성 버튼**은 좌측 패널 하단에 항상 고정 노출(스크롤해도 안 사라짐). 누르기 전 "최종 프롬프트 미리보기" 팝업/패널이 항상 뜨고, 확인을 눌러야 실제 생성이 시작됨.

**C. 중앙 캔버스**
- 현재 패널 미리보기(생성 결과), 로딩 중에는 진행률 표시(어느 워크플로우 노드를 처리 중인지 표시)
- 결과 위에서 바로 인페인팅(손/얼굴 보정 영역) 지정 가능
- 이전 패널 ↔ 다음 패널 이동, 페이지 내 컷 순서 재배열(드래그)

**D. 우측 기록/갤러리 패널**
- 이번 세션에서 생성한 이미지 히스토리(그리드), 즐겨찾기(북마크), 프로젝트별 필터
- 각 썸네일에 사용된 Seed/프롬프트/워크플로우 메타데이터 표시(재사용 버튼 포함)

**E. 만화 에디터 화면 (별도 탭, 기존 v2 문서 #32~#35 사양 유지)**
- 페이지 레이아웃, 말풍선, 대사, 효과음(SFX) 배치, Export(웹툰용 롱스트립 이어붙이기 / 페이지 PDF·이미지 시퀀스)

### 3-3. 필수 요건

- **모든 버튼/토글은 실제로 백엔드 기능과 연결되어 정상 동작해야 한다.** (죽은 버튼 금지)
- 각 기능은 이름만 봐도 뭘 하는지 알 수 있게 라벨링하고, 3-2에 명시된 위치를 벗어나 임의로 재배치하지 않는다.
- 4단계 완료 시, **UI 항목 ↔ 실제 기능 매핑표**를 만들어서 각 항목이 "정상 작동 확인됨"인지 체크리스트로 제출한다.

---

## 4. 패키징 및 배포 방식 (신규)

### 4-1. 문제

지금까지는 결과물을 압축파일(zip)로만 전달받았다. 압축을 풀고, 경로를 맞추고, 의존성을 수동으로 설치하는 과정이 번거롭고 실수가 잦다. 이전에 별도로 만들어졌던 "Manga Scene Studio for ComfyUI 1.0.0" 설치 마법사(기존 ComfyUI 폴더를 선택해서 커스텀 노드/워크플로우 1개만 추가하는 방식)가 있었는데, 이번 최종본에서는 **프로그램 전체**를 이 수준의 설치 경험으로 확장한다.

### 4-2. 요구사항

- 최종 산출물은 **zip이 아니라 실행 가능한 설치 프로그램(.exe) 1개**여야 한다. (Inno Setup 또는 NSIS 사용 권장)
- 설치 마법사는 일반적인 게임/상용 프로그램과 동일한 수준으로 구성한다.
  - 설치 경로 선택(기본값 제공)
  - **바탕화면 바로가기 생성** 옵션(기본 체크)
  - 시작 메뉴 등록
  - 설치 진행률 표시
  - 설치 완료 후 "지금 실행" 옵션
- **삭제(제거)도 표준 방식**으로 가능해야 한다: Windows "설정 → 앱 → 앱 및 기능"(또는 제어판 "프로그램 추가/제거")에 정상 등록되어, 거기서 바로 제거할 수 있어야 한다. 별도 언인스톨러(`Uninstall.exe`)도 함께 설치되도록 한다.
- 제거 시 사용자가 만든 작업물(생성된 이미지, 프로젝트 파일)은 **기본적으로 보존**하고, 삭제 여부를 묻는 확인창을 띄운다. (프로그램 파일만 삭제 / 작업물까지 전부 삭제, 2가지 옵션)
- ComfyUI 의존성 처리 방식은 아래 중 하나를 명확히 선택해서 구현하고, 어떤 방식을 택했는지 보고서에 명시한다. (임의 선택 금지, 애매하면 먼저 질문할 것)
  - (A) 기존에 설치된 ComfyUI 폴더를 사용자가 지정하게 하고 커스텀 노드/워크플로우만 설치 (기존 방식 유지·확장)
  - (B) ComfyUI를 포함해 필요한 것을 전부 한 번에 설치하는 완전 독립형 설치 (권장 — 반복되는 "환경 안 맞음" 문제를 원천 차단)
- 아이콘, 제품명, 버전 정보, 게시자 이름을 설치 파일에 정상적으로 채워 넣는다. (다음 5장의 보안 경고 완화와도 직결됨)

---

## 5. Windows 보안 경고(SmartScreen) 대응 (신규)

### 5-1. 배경

코드 서명이 안 된 실행파일을 인터넷에서 받아 실행하면, Windows Defender SmartScreen이 "Windows에서 PC를 보호했습니다" 같은 경고를 띄우고 실행을 막는 경우가 흔하다. 개인/소규모 배포 소프트웨어에서 자주 겪는 문제이며, 미리 대응해두고 싶다.

> **참고**: 첨부한 2번째 스크린샷은 실제로는 ComfyUI의 시스템 사양 정보(OS/Python/PyTorch/RAM/GPU) 화면으로 보이며, SmartScreen 경고 화면 자체는 아닌 것 같다. 내가 말하려던 경고 문구(설치파일 다운로드/실행 시 뜨는 보안 경고)가 정확히 이 화면이 아닐 수 있으니, 아래 요구사항은 **일반적인 SmartScreen 경고에 대한 대응**으로 우선 진행하고, 실제로 다른 문구/화면이 뜨면 스크린샷을 다시 보내서 알려주겠다.

### 5-2. 요구사항

- 가능하다면 코드 서명 인증서(EV 또는 OV)로 설치 파일에 서명한다. 비용이 발생하는 부분이니, **서명 없이 배포했을 때 실제로 어떤 경고가 뜨는지**와 **서명 시 필요한 절차/예상 비용**을 먼저 조사해서 알려주고, 서명 여부는 그 정보를 보고 결정한다.
- 서명이 당장 어렵다면, 최소한 아래로 신뢰도를 높인다.
  - 설치 파일에 제품명/버전/게시자/아이콘 등 정상적인 메타데이터를 채운다.
  - 설치 프로그램 자체는 잘 알려진 툴(Inno Setup/NSIS)의 표준 방식대로 만들어, 임의 실행파일보다 신뢰도가 높은 형태로 만든다.
- SmartScreen 경고가 뜨는 상황을 자동으로 피할 수 없는 경우, **설치 안내 문서(또는 설치 화면 자체)에 "추가 정보 → 실행" 우회 절차를 스크린샷과 함께 미리 포함**해서, 경고가 떠도 사용자가 당황하지 않게 안내한다.
- 실제로 배포용 설치 파일을 만든 뒤, 다른 PC(또는 가상환경)에서 다운로드→실행까지 재현해보고 어떤 경고가 뜨는지 캡처해서 보고에 포함한다.

---

## 6. 최종 완료 기준 (DoD) 및 보고 형식

작업 완료 시 아래 항목을 모두 체크해서 제출한다.

- [ ] 한국어 로컬 LLM 파싱 파이프라인 동작 (OpenAI 키 없이도 정상 동작 확인)
- [ ] 생성 전 프롬프트 미리보기/수정 UI 동작
- [ ] `통합_자연어_IPAdapter_ControlNet.json` 링크 오류 수정 완료 + 그래프 검증 스크립트 통과 결과
- [ ] `/prompt` 실제 큐 실행 성공 로그/스크린샷
- [ ] `patch_*.py` 정리 후 코드 구조(폴더/모듈 구성) 설명
- [ ] 3장의 UI 전 항목이 실제로 동작하는지에 대한 매핑 체크리스트
- [ ] 설치 파일(.exe) 1개로 배포, 바탕화면 바로가기·시작메뉴 등록 확인 스크린샷
- [ ] Windows "앱 및 기능"에서 정상 제거되는지 확인 스크린샷
- [ ] 서명/SmartScreen 조사 결과 및 대응 여부
- [ ] `validate_stage2.py` 기준 필수 커스텀노드/모델 설치 확인 결과

---

**요약**: 기존 두 문서에서 확정된 백엔드·모델·캐릭터 일관성 사양과, 아직 해결 안 된 한국어 NLU·워크플로우 링크·코드 구조 문제는 그대로 승계한다. 여기에 이번 최종본에서 ① 화면 구성까지 명시한 UI 사양, ② zip이 아닌 표준 설치/삭제 프로그램 패키징, ③ Windows 보안 경고 대응까지 추가로 확정했다. 이 문서 하나로 처음부터 끝까지 재작업 없이 완결되는 버전을 만들어 달라.
