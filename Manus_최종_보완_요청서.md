# AI Webtoon Studio — 최종 보완 요청

지금까지 여러 버전을 만들어봤는데, 실제로 써보니까 문제가 몇 가지 확인됐어. 이번에 한 번에 정리해서 제대로 된 버전으로 만들어줘. 아래 문제들은 전부 실제로 파일을 열어서 확인한 내용이야.

---

## 1. 가장 먼저 고쳐야 할 것 — 한국어 자연어 이해 방식

지금 방식(고정된 80개 정도의 한국어 구문 → 영어 치환 사전, `translate_helper.py`)은 사전에 없는 표현은 그냥 무시돼. 이러면 내가 어떤 식으로 문장을 써도 제대로 안 먹히는 게 당연해. **이 방식을 기본값으로 쓰지 말고**, 아래처럼 바꿔줘.

- **기본(항상 작동)**: 로컬에서 도는 한국어 지원 소형 LLM(예: Qwen2.5 계열, EXAONE, Bllossom 등 GGUF 양자화 버전)을 llama.cpp 같은 걸로 CPU에서 돌려서, 한국어 문장을 장면 구조(인물/포즈/의상/시선/배경/카메라)로 분석하고 영어 프롬프트로 변환하게 해줘. RAM은 32GB 있으니까 여유 있고, 이미지 생성은 GPU를 쓰니까 CPU에서 도는 이 작업이랑 충돌 안 나.
- **선택(있으면 더 좋음)**: `OPENAI_API_KEY`가 등록돼 있으면 그걸 우선 써도 되는데, **없어도 위 로컬 LLM 경로로 항상 정상 작동해야 해.** 지금처럼 "키 없으면 사전 매칭으로 조용히 다운그레이드"는 안 돼.
- 어떤 경로로 처리됐는지(로컬 LLM / OpenAI) 화면에 항상 표시해줘. 그리고 **생성 누르기 전에 실제로 어떤 영어 프롬프트가 만들어졌는지 미리 보여주고**, 내가 확인/수정한 다음 생성하도록 해줘. 지금처럼 뒤에서 조용히 처리되다가 이상한 결과가 나오는 걸 막고 싶어.
- 사전에 없는 표현이라고 그냥 버리지 말고, 최소한 "이 부분은 이렇게 해석했다"를 보여줘서 내가 확인할 수 있게 해줘.

## 2. "통합_자연어_IPAdapter_ControlNet.json" 워크플로우 자체가 고장 나 있었음

이거 하나가 실제 원인이었을 가능성이 높아. 직접 그래프를 파싱해서 확인했더니:

- **KSampler의 `model` 입력이 LoadImage(참조 이미지)의 출력에 연결돼 있음** — MODEL이 들어가야 하는데 IMAGE 데이터가 들어감. 타입이 아예 안 맞음.
- **IPAdapterAdvanced의 `image`(참조 이미지) 입력이 KSampler의 출력(LATENT)에 연결돼 있음** — 원래 LoadImage에서 와야 하는데 완전히 다른 데이터가 연결됨.

정리하면 이렇게 고쳐야 해:

```
KSampler.model  ←  IPAdapterAdvanced(#6)의 MODEL 출력
IPAdapterAdvanced.image  ←  LoadImage(#5, 캐릭터 참조 이미지)의 IMAGE 출력
```

**다른 3개 파일(기본생성.json / 참조이미지_img2img.json / ImpactPack_얼굴손_Detailer.json)은 그래프 검증해봤는데 이상 없었어.** 이 파일들은 그대로 둬도 돼.

## 3. 앞으로는 워크플로우를 보내기 전에 그래프 검증을 항상 돌려줘

이런 링크 꼬임이 또 생기지 않게, 워크플로우 JSON을 만들거나 수정할 때마다 **다음 두 가지를 만들어 보낸 파일에 확실히 통과시켜줘.**

1. 각 노드 입력의 `link` id가 가리키는 링크의 **target이 실제로 그 노드가 맞는지**, **타입이 노드가 기대하는 타입과 일치하는지** 검사하는 스크립트 (이미 `fix_workflow_links.py`, `validate_stage2.py` 같은 게 있던데, 이런 검증을 워크플로우 출고 전에 항상 실행하고 결과를 같이 보내줘)
2. 가능하면 실제로 ComfyUI API에 `/prompt`로 큐를 넣어서 **에러 없이 실행까지 되는지** 한 번 확인하고 보내줘. JSON이 문법적으로 맞다고 해서 그래프가 맞는 건 아니라는 걸 이번에 확인했어.

## 4. 코드가 patch_*.py 방식으로 계속 땜질되고 있음

압축 풀어보니까 `patch_*.py`, `fix_*.py` 종류가 50개 가까이 있더라. 계속 이런 식으로 위에 덧붙이지 말고, **이번 최종본에서는 한 번 정리해서 깔끔한 구조로 재정리**해줘. 특히 UI 관련(`patch_white_ui.py`, `patch_ui_final_polish.py`, `patch_cache_blank_screen.py`, `fix_removed_scene_button.py` 등)은 같은 문제가 반복 발생했었다는 뜻이니까, 원인을 근본적으로 고치고 패치를 쌓지 않는 방향으로 가줘.

## 5. 백엔드/모델 관련 — 이미 확인된 건 그대로 유지

- **ROCm 백엔드는 이미 네이티브로 잘 돌고 있음** (`PyTorch 2.12.0+rocm7.14.0`, RX 6600 정상 인식, VRAM 7.84GB free 확인됨) — 추가로 DirectML/ZLUDA 설치할 필요 없음.
- 체크포인트는 지금 쓰고 있는 **Animagine XL 4.0**으로 계속 가도 됨 (검증된 괜찮은 애니메 SDXL 모델임). 급하게 바꿀 필요 없고, 시간 되면 NoobAI-XL / WAI-Illustrious-SDXL 계열과 품질 비교 정도만 나중에 해봐도 됨.
- 얼굴/손 보정(ImpactPack Detailer)은 파일 검증 결과 정상이니 그대로 사용.
- ControlNet(OpenPose)·IPAdapter는 필요한 커스텀 노드(`ComfyUI_IPAdapter_plus`, `comfyui_controlnet_aux`, `ComfyUI-Impact-Pack`, `ComfyUI-Impact-Subpack`)와 모델(`OpenPoseXL2.safetensors`, `bbox/face_yolov8m.pt`, `bbox/hand_yolov8s.pt` 등)이 실제로 설치됐는지 `validate_stage2.py`로 확인하고, 결과를 알려줘.

## 6. 확인/보고 방식

이번 최종본을 보낼 때는 다음을 같이 알려줬으면 해:

- 위 1~5번 각각 어떻게 처리했는지 (됐음/부분적으로 됐음/못 했음 + 이유)
- 워크플로우 그래프 검증 스크립트 실행 결과
- 실제로 ComfyUI에서 한 번 생성까지 성공한 스크린샷이나 로그 (가능하면)

---

**요약**: 지금까지 생긴 문제는 크게 (1) 한국어 처리가 고정 사전이라 임의의 문장에 약함, (2) 통합 워크플로우 파일 자체의 링크 연결 오류, (3) 검증 없이 배포되는 구조, 이 세 가지가 겹친 거였어. 이번엔 이 세 가지를 확실히 해결한 버전으로 부탁해.
